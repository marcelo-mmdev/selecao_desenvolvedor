namespace Models
{
    // Enum que define os tipos possíveis de transações
    public enum TipoTransacao
    {
        Credito,
        Debito,
        TransferenciaEnviada,
        TransferenciaRecebida,
        PagamentoBoleto
    }
}